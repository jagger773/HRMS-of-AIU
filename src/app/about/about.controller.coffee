angular.module 'stock'
.controller 'AboutController', ($timeout, toastr,$translate,$state) ->
    'ngInject'
    vm = this

    return

.controller 'AboutExpertController', ($timeout, toastr, $translate, $state, Session) ->
    'ngInject'
    vm = this


    return
